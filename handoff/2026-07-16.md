# Handoff — 2026-07-16

Gün 26B/12B sesli A/B testi için başladı, **A/B tamamlanamadı**. Yol boyunca beş altyapı
hatası çıktı, hepsi çözüldü. Tarayıcı istemcisi terk edildi, yerine terminal istemcisi geldi.

> ⚠️ **HİÇBİR ŞEY COMMIT EDİLMEDİ.** Aşağıdaki 18 dosya çalışma ağacında, review bekliyor.
> `git` bu makinede Xcode lisansı yüzünden çalışmıyor: `sudo xcodebuild -license` gerekiyor.

---

## 1. Şu anki canlı durum

| | |
|---|---|
| **Beyin** | **Gemma-4-26B-A4B** (MoE ~4B aktif, UD-IQ4_XS) `.25:8082`, `--ctx-size 65536 --parallel 1` |
| 12B'ye dönüş | `cp /etc/systemd/system/candan-brain.service.bak-12b-live → candan-brain.service` + `daemon-reload` + `restart` |
| VRAM | 22.6 / 24.5 GB |
| İstemci | **terminal** (`worker/go.sh`) — web UI kullanılmıyor |
| Log'lar | `worker/logs/agent.log` + `worker/logs/transcript.log` (her başlatmada sıfırlanır) |

**Kullanım:** `cd worker && ./go.sh` (worker arka planda + istemci ön planda).
`lk agent dev` KULLANMA — `AgentServer` deseni ister, `agent.py` uyumsuz, üstelik modülü
`__main__` yerine import ettiği için log dosyası kurulumu hiç çalışmaz.

---

## 2. 26B vs 12B — eldeki veri (A/B YARIM)

**26B lehine (ölçüldü):**
- Temiz decode **115 tok/s** (12B: 55-63). Uzun bağlamda yavaşlamıyor (32k+ → 115).
- `reminder_add` **24/24**, her sıcaklıkta (temp 0.8 dahil). Sampling'e dokunmaya gerek yok.
- `memory_add` scope **taşıma** semantiğini kusursuz yaptı (private → family, kopyalamadan).
- Kullanıcı sesli izlenimi: "daha güzel, daha duygulu".

**26B zayıf yanları:**
- `soul_add`: "bundan sonra" kalıbı yoksa çağırmıyor → bkz [[soylemeden-once-yap]] (kural eklendi).
- `[mood:sad]` eşiği yüksek — mekanizma sağlam, model seçmiyor.
- Tool hata sonucunu yutuyor: `soul_add` → `"guest: ruh kaydı yok"` dönerken "Tamamdır" dedi.

**GEÇERSİZ SAYIN:** gece "12B hatırlatıcıyı çağırmadı, 26B çağırdı" bulgusu **n=1**'di.
Ölçüm harness'i eksikti (`at` parametresi yoktu). Bu iddiaya dayanma.

**A/B'yi tamamlamak için:** `scratchpad/ab-diyalog.md` (9 blok, skor tablosu) + `.25`'te duran
`/root/tool_secim_testi.py`, `/root/canli_sira.py`, `/root/brain_latency.py`, `/root/dagilim.py`.
12B turundan ÖNCE hafıza temizliği şart (kahve notu `memory/family.md`'de duruyor).

---

## 3. Çözülenler (hepsi uncommitted)

| konu | dosya | not |
|---|---|---|
| Telaffuz: şimdi/şu an/işine | `.25:/opt/omnivoice/pronounce_tr.json` + `bridge_server.py` | **repo dışı**; sol-taraf `\b` sınırı eklendi, yoksa "konuşuyor" bozuluyordu |
| Mood TTS çökmesi | `worker/omnivoice_tts.py:250` | `self._http_url()` → `self._omni._http_url()` |
| Context duvarı (32k) | `.25:candan-brain.service` | `--parallel 2` → **1**; VRAM maliyeti sıfır, context 2× |
| **Compaction ölü doğmuştu** | `~/.pi/agent/models.json` | `supportsUsageInStreaming: false` **yalandı** → `true`. 0 → 2 compaction, prompt 34k → 8.2k |
| **Zombi oturum** (2 günlük acı) | `worker/agent.py` | `session.on("close")` → `ctx.shutdown()` |
| Log dosyası | `worker/log_utils.py`, `agent.py` | `AGENT_LOG_FILE`, ana süreçte truncate |
| Terminal istemcisi | `worker/cli_client.py`, `go.sh` | token yerelde, `/api/token` gerekmez |
| Eylem kartı argümanları `{}` | `worker/pi_brain.py:1145` | yalnız `message_end`/`turn_end`'de yayınla |
| **Tool sırası + rol yapma** | `worker/pi_brain.py:112-133` | 0-5/20 → **20/20** |
| Soğuk ilk tur stall | `worker/pi_brain.py`, `.env` | ilk delta'ya kadar 45s, sonra 12s + "aklımı topluyorum" |
| Çift transkript | `worker/cli_client.py` | LiveKit `is_delta_stream=False` iki stream yayınlıyor → `final` filtrele |
| Ses/metin hizalama | `worker/cli_client.py` | `read_all()` hizalamayı çöpe atıyordu → artımlı bas |
| **Echo cancellation** | `worker/cli_client.py` | WebRTC APM; `--no-aec`, `--half-duplex` yedeği |
| **İsim ayrıştırma** | `worker/name_parser.py` | **16/32 → 32/32**; "Havi adım" düzeldi, "Efendim/Anlamadım" artık isim sanılmıyor. Test: `python pi_brain.py name` |
| Enrollment: 3 deneme + sessiz düşüş yok | `worker/pi_brain.py` | 2. soru daraltıyor; vazgeçerken tek cümle bilgi. Başarısız metin artık loglanıyor (`text[:60]`) |
| Transkript etiketi | `worker/cli_client.py` | Worker konuşmacıyı odaya YAYMIYOR (kanıtlandı) → etiket nötr **"Sen"**. `--me` artık sadece LiveKit katılımcı adı; `--me-label` ayrı. İleride worker `candan.speaker` yayarsa bağlanacak yer orası |
| Web: yeni oturum + kopyala | `web/components/app/*` | **hiç test edilmedi** |

---

## 4. Açık işler

1. **`web_search` düzeltmesi yanlış dosyaya yapıldı.** `pi/extensions/websearch/index.ts`
   = `WEB_SEARCH_LEGACY_QWANT` arkasında, **kapalı**. Canlı arama `@oresk/pi-searxng`
   (`~/.pi/agent/npm/node_modules/@oresk/pi-searxng/searxng.ts`, repo dışı).
   Ölçüm geçerli (dar açıklama → "Potterspar nedir?" 0/6; geniş → 6/6), uygulama yeri yanlış.
   `AGENTS.md`'deki "bilmiyorsan ara" kuralı boşluğu kapatıyor olabilir — **ölç**.
2. **Model tool HATA sonucunu yutuyor — prompt ile ÇÖZÜLEMEZ (ölçüldü).**
   `soul_add` → `"guest: ruh kaydı yok"` dönerken Candan *"Tamamdır Havi, bundan sonra..."* dedi.
   AGENTS.md'ye kural eklemeyi denedik, **ölçüm reddetti**: kural YOK → 10/10 uydurma,
   kural VAR → **10/10 uydurma**. Fark sıfır, kural EKLENMEDİ.
   Üç prob: (a) tool sonucu modele ULAŞIYOR (birebir tekrarlatılabiliyor), (b) "kayıt oldu mu?"
   diye sorunca **"Evet"** diyor — metni okuyor, hata olduğunu **anlamıyor**, (c) tool mesajı
   çok daha sert yapıldı (`"HATA: kayıt YAPILMADI... kaydettiğini SÖYLEME"`) → sadece 1/10 düzeldi.
   **Önerilen çözüm (yapılmadı, onay bekliyor): kod seviyesinde kapı** — guest'ken
   `soul_add`/`memory_add` sonucunu modele bırakma, scripted satırla cevapla (enrollment'ın
   zaten yaptığı gibi). Bu, 26B'nin bilinen bir zaafı; 12B'de de ölçülmeli.
3. **Job süreci ~950 MB sızdırıyor** (142 MB → 1090 MB / 20 dk). LiveKit "advisory" diyor.
   Ev asistanı günlerce açık kalacaksa bakılmalı.
4. **Web UI hiç test edilmedi** (yeni oturum butonu, kopyala butonu). Kullanıcı tarayıcı açmıyor.
5. **Çok-konulu oturum** fikri (kullanıcının): konu değişince ajan "ayrı sohbete alayım mı" diye
   sorsun. Tasarım kararı: **model fark eder ve SORAR** (konuşma işi), **deterministik ifade
   eşleşmesi YÜRÜTÜR** (tool'a bel bağlama). 3 kullanıcıya geçerken `--parallel 3` +
   `contextWindow` 20480'e çekilmeli.
6. **12B karşılaştırması yapılmadı.** Kullanıcı 26B'yi doyasıya test etmek istedi.

---

## 5. Tuzaklar — okumadan iş yapma

- **`git` çalışmıyor** (Xcode lisansı). Ajanlara "gerekmiyor" de.
- **Ajanları kullanıcının dev sunucusu açıkken `web/`'e salma** → `.next` bozulur. Bugün iki kez
  oldu; izole worktree kullan.
- **`~/.pi/agent/settings.json` GLOBAL** — kullanıcının kodlama pi'si onu kullanıyor. Proje ayarı
  için `<repo>/.pi/settings.json`.
- **`go.sh` `.env`'i source etmez** — `agent.py`/`cli_client.py` ikisi de `load_dotenv()` yapıyor.
- **Sesli/görsel testi ajanlara yaptırma** — kullanıcı yapar; ajan yalnız build/lint doğrular.
- **Ölçüm hataları:** bugün 5 kez kendi harness'im yüzünden yanlış alarm verdim
  (BSD `find -newermt`, eksik tool şeması, taze sanılan oturum, eksik promptSnippet, ölü dosya).
  Bkz `memory/kontrol-deneyi-dersi.md` — N≥8, çelişkide N≥20 + 2×2 kontrol.

---

## 6. Not defteri (yeni)

`memory/` altında yazıldı: `tool-sirasi-rol-yapma`, `soylemeden-once-yap`,
`compaction-ve-context`, `terminal-istemcisi`, `zombi-oturum`; `kontrol-deneyi-dersi`
güncellendi. `MEMORY.md` indeksi güncel.

---

## 7. Yeni oturumda ilk adımlar

```bash
cd /Users/drascom/work/candan-lite
cat handoff/2026-07-16.md          # bu dosya
tail -40 worker/logs/agent.log     # canlı durum
cd worker && ./go.sh               # test etmek için
```
Sonra: (a) enrollment ajanının işini bitirdi mi bak, (b) `web_search`'ü doğru yere taşı ya da
kuralın yettiğini ölç, (c) 26B testine devam / 12B'ye geç.
