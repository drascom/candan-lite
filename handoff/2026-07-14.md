# HANDOFF — 2026-07-14

## Mimari (bugün değişti)

**Ses → Gemma-4-12B → tool/cevap → ses.** Tek katman.

Beyin artık **yerel**: Gemma-4-12B-it QAT (q4_0), llama-server, `192.168.0.25:8082`,
systemd `candan-brain.service`. pi.dev sağlayıcı olarak bağlı.

**Model değiştirme = tek satır** (`worker/.env`):
```
PI_MODEL=llama-cpp/gemma-4-12B-it-qat-q4_0   + PI_THINKING=default   # yerel (varsayılan)
PI_MODEL=openai-codex/gpt-5.6-terra          + PI_THINKING=minimal   # uzak (geri dönüş)
```
Sağlayıcı tanımı `~/.pi/agent/models.json` → `providers."llama-cpp"` — **repo DIŞI**,
yeni makinede yeniden yazılmalı.

**Gecikme:** sohbet 2.76 sn → **0.35 sn** · tool turu 5.10 sn → **1.86 sn**
**VRAM (ölçüldü):** beyin 10.1 + Whisper 2.4 + TTS 4.7 + moss-diarize 2.0 ≈ **19 / 24.5 GB**

## İki operasyonel tuzak (ikisi de canlıda ısırdı)

1. **`llama-server --cache-ram 0` ZORUNLU.** Varsayılan 8 GB host RAM ister →
   **sessiz OOM-kill** (logda hata yok, süreç kaybolur).
2. **Gerçek pi prompt'u ~17.5k token.** Slot 8k iken llama-server *her* isteği reddediyordu
   → kullanıcı "Bir saniye, tekrar dener misin?" yedeğini duyuyordu.
   Şu an: `--ctx-size 65536 --parallel 2` (2 slot × 32k).

## Bugün uygulananlar

- **Yerel beyin** (yukarıda) — router, çeviri katmanı ve niyet analizi kaldırıldı (`7edc998`).
  Beyin tool seçimini kendi yapıyor.
- **`wake_stt` üst sınırı** (`cdd17b8`): `WAKE_STT_MAX_SECONDS=4.0`. Uykuda 9 sn'lik cümle için
  Whisper'a giden ses ~40 sn → ~8.5 sn (**%79 az**).
- **Dashboard** (`tools/dashboard.py`, `python3 tools/dashboard.py` → :8765):
  oturum dökümü + **salt-okunur** hafıza paneli (`memory/`, `events.db`, `speakers.db`).
  Hafızaya yazma/silme yolu YOK.
- **Persona**: `pi/AGENTS.md`'ye "takip cümlesi yasak" ilkesi — "Başka bir isteğin var mı?"
  türü boş nezaket cümleleri kalktı; gerçek sorular korundu.
- **`docs/MULTI-CLIENT-PLAN.md`** (`da5f066`): Pi hoparlör + iOS + Mac + web sözleşmesi.
- **`docs/ROUTER-EXPERIMENTS.md`**: model/motor seçim gerekçeleri (kalıcı kayıt).
- **`experiments/router-bench/`**: 137 cümlelik test seti + `tool_catalog.py` (30 tool, low/high
  tier) + harness'ler. **Beyni değiştirmek istersek ölçüm aleti bu — SİLME.**

- **Hatırlatma teslim yarışı düzeltildi.** Candan "hatırlatmam var" der, kullanıcı "efendim" der,
  ama hatırlatma söylenmez; cümle pi'ya düşüp yeni soru sanılırdı — ve `events.db`'de
  **`delivered` yazılırdı** (hatırlatma kaybolurdu). Kök sebep: onay, sözün *başlaması*
  (VAD) sayılıyordu; final transkript saniyeler sonra gelince `hold` kapısı çoktan
  kapanmış oluyordu. Şimdi onay = sözün *bitmesi*; hatırlatma kesilirse `delivered`
  işaretlenmez, pending kalır. 5 yeni test + regresyon kanıtı.
  (Yarış yapısaldı — yeni beynin hızı sadece görünür kıldı.)

## Devam eden (bu oturum kapanırken worker'da)

- **Web'de beyin seçici** — oturum başında yerel/uzak model seçimi (localStorage, varsayılan yerel).
- **Web sohbetinde tool görünümü** — tool çağrısı + sonucu `mate.*` topic'iyle odaya yayınlanıp
  dashboard'daki gibi render edilecek. `(Sistem: şu an ...)` öneki arayüzde gizlenecek.

## Planlanan

- **Tool extension'lar** — asıl iş. Bugün pi'de yalnızca gerçek tool'lar var (hafıza, hatırlatma,
  web arama). Ev otomasyonu (ışık, timer, medya) yok. Onlar geldiğinde:
  - **`light_control` için router-dışı koruma şart**: "perdeleri kapat" gibi cümleler yanlış
    tool'a yapışabiliyor (Türkçede "kapat" = hem *turn off* hem *close*). Gemma-4 bunu
    benchmark'ta çözdü ama gerçek tool eklenince canlıda yeniden doğrulanmalı.
  - **Yetki kademelemesi**: geri alınamaz tool'lar (para, mesaj, silme) executor'da onaya bağlı.
- **Konuşma ortasında GPT'ye devretme** — tool extension'lar oturduktan sonra tasarlanacak.
  **MEKANİZMA ZATEN VAR:** `pi_brain` pi alt-sürecini `--session-dir`/`--session-id` ile
  başlatıyor ve `_build_pi_args(persona, session_id, model, thinking)` içinde **model bir
  parametre**. Konuşma ortasında pi'yı öldürüp yeniden doğurma da çalışıyor — speaker-ID
  konuşmacıyı tanıyınca persona'yı böyle değiştiriyor (`pi swap: candan → ayhan`), konuşma
  kopmuyor (oturum `--session-id` ile sürüyor). Yani model değiştirmek AYNI kod yolu:
  worker/agent yeniden başlamaz, oda kopmaz, sadece pi alt-süreci değişir.
  **Asıl zorluk teknik değil, BAĞLAM TAŞIMA:** uzak modele hangi tool tanımları, hangi
  hafıza, geçmişin ne kadarı gidecek?
- **5 hoparlörlü ölçek**: gerçek prompt 17.5k token olduğu için 5 eşzamanlı oturumda KV patlıyor.
  Kaldıraçlar: KV quantize (q8_0, KV'yi yarıya indirir), pi prompt'unu kırpma, daha az slot.
- **PREFIX CACHE / persona swap maliyeti (çok-kullanıcılıda ilk duvar):**
  `_build_pi_args()` HER pi doğuşunda sistem prompt'unu dosyalardan yeniden kuruyor
  (`AGENTS.md` → `personas/<kişi>.md` → `memory/soul.md` → `users/<kişi>/profile.md` →
  `memory/family.md` → `users/<kişi>/soul.md`). Persona değişince (speaker-ID başka birini
  tanıyınca → `pi swap`) prompt TAMAMEN değişir → llama-server prefix cache ıskalar →
  **soğuk prefill ~3.5 sn** (sıcak 184 ms). Slot sayısı 2 → üçüncü kişi konuşunca tahliye.
  **Öneri (ölçülmedi):** prompt'un DEĞİŞMEYEN kısmını (AGENTS.md + ortak soul + family) BAŞA,
  kişiye özel kısmı (persona + profile + kişisel soul) SONA al → tüm kişiler ortak bir prefix
  paylaşır, sadece kuyruk yeniden işlenir. Önce mevcut sıraya bak.
- **Çok-istemcili plan** (`docs/MULTI-CLIENT-PLAN.md`): Faz 1 = telefondan konuş
  (mevcut iOS app'i lite'a bağla, token/dispatch, `canUpdateOwnMetadata` eksiği).

## Bilinen arızalar

- **`web_search` çalışmıyor**: Qwant **CAPTCHA** döndürüyor (kanıt: eski `r1.json`/`r2.json`
  captcha sayfalarıydı). Sağlayıcı değiştirmek ya da captcha'yı aşmak gerekiyor.
- **LiveKit cloud turn detector 401** — anahtar geçersiz, yerel mini modele düşüyor. Çalışıyor
  ama tur tespiti muhtemelen daha zayıf.
- **Bağlantı**: worker restart sonrası tarayıcı eski bağlantıyı tutuyorsa agent odaya girmiyor
  (yeni token basılmadığı için dispatch tetiklenmiyor). Sekmeyi tam kapatıp açmak çözüyor.

## Sunucu

`ssh root@192.168.0.25` — RTX 3090 (24 GB), 6 çekirdek, 19 GB RAM.
Servisler: `candan-brain.service` (:8082), `whisper.service` (:10300), OmniVoice TTS (:8808),
moss-diarize. Ollama **emekli** (durduruldu, modelleri silindi).
