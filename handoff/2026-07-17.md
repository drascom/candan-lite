# Handoff — 2026-07-17

Uzun bir oturum. Ana konu: **speaker-ID'nin gerçekten çalışması** (ev sahibi Ayhan ile eşi Havva'yı
ayırmak) + gün içinde birkaç altyapı düzeltmesi. Kişi sesle konuşuyor; eşinin adı asistanda **"Havi"**
(STT "Havi/Hobi/Havva" hepsi aynı kişi = Havva).

---

## TL;DR — şu anki durum

- **Sunucu `.25`**: worker/brain/dashboard üçü de `active`. Worker artık **systemd** ile kalıcı
  (`candan-worker.service`, `Restart=always`). Git deploy: push (Mac) → `git pull` (sunucu) → restart.
- **Encoder: WeSpeaker ResNet34** (campplus'tan geçildi, dim 256). **AS-norm kalibrasyonu CANLI ve aktif**
  (`_asnorm_active=True` doğrulandı). **auto-learn KAPALI** (`SPEAKER_LEARN_ENABLED=false`).
- **speakers.db BOŞ** (0 kişi) — temiz sayfa, kimse kayıtlı değil.
- **Sunucu git: `5471f51`**; Mac `9996e60` (aradaki tek commit `experiments/`'e, canlı worker'ı
  etkilemez ama sunucuda `git pull` iyi olur).
- **AS-norm eşiği `-1.0`** (kod default, .env override yok) — TAHMİN, ölçülmedi, ayarlanacak (aşağıda).

---

## BUGÜN YAPILAN

### Altyapı
- **Worker → systemd** (`candan-worker.service`): ssh kapanınca ölmüyor, çökünce 5sn'de kalkıyor.
  Dün geceki "ses gidip gelmiyor" bunun eksikliğindendi (elle başlatılan worker ssh'la ölmüştü).
- **Dashboard `/log` ekranı** (`tools/dashboard.py`): `192.168.0.25:8765/log` — worker log'u,
  seviye/logger filtresi, AJAX tazeleme (meta refresh değil). LAN'a açık (kullanıcı onayladı, kimlik
  doğrulama yok). `tail_lines()` dosyayı sondan okuyor (206MB'ı 0.001sn). Systemd: `candan-dashboard`.
- **Worker log: truncate → rotate** (`log_utils.py`, `RotatingFileHandler` 10MB×3). `Restart=always`
  altında `mode="w"` çökme kanıtını siliyordu.
- **İstemci uyku/uyanma çanı + belirteci** (`cli_client.py`): `candan.awake` attribute'unu dinliyor,
  web'le aynı ton (uyan 660→990Hz, uyu 660→440). `--dump-audio` bayrağı da eklendi (giden mikrofonu
  16k WAV'a yazar — corpus toplamak için).
- **speaker-ID log gürültüsü**: tanınınca INFO, tanınmayınca DEBUG (saniyede bir satır boğuyordu).

### Hafıza taşındı, temizlik başladı
- Mac'teki `memory/` ve `worker/data/` **arşivlendi** (`~/candan-lite-arsiv/`), Mac'ten kaldırıldı.
  **Tek kaynak artık SUNUCU.** Okuma+yazma doğrulandı (Candan sunucuda hafızaya yazıyor).
- **Ayhan'a dair her şey silindi** (kullanıcı isteğiyle, arşivlendi): eski kayıt, policy, notlar,
  eventler. Sıfırdan başlıyoruz.
- Geriye dönük genel temizlik kullanıcı tarafından İSTENDİ ama ERTELENDİ → `[[planli-geriye-donuk-temizlik]]`.

### Speaker-ID — günün ana işi (uzun yolculuk, birkaç yanlış hedef)
1. **Kayıt sihirbazı yeniden yazıldı** (`pi_brain.py` + `pi/extensions/speaker-enroll/`): LLM sürüyor,
   kod denetliyor/yazıyor (harness mimarisi). Çok-örnekli toplama + kalite seçimi + isim onayı +
   emniyet ağı (model tool çağırmazsa kod devreye girer). TS imza hatası bulundu+düzeltildi (`tsc`).
2. **Toplayıcı yankı/sessizlik elemesi** (`ba3de8b`): sadece `user_speaking AND not agent_busy`
   pencere alır → Candan'ın TTS yankısı centroid'i zehirlemez.
3. **Encoder A/B**: campplus GERÇEK Ayhan sesinde aynı-kişi 0.39 (medyan 0.43 < eşik 0.45!),
   WeSpeaker 0.63. campplus duygu/tonlamadan etkileniyor (aile asistanında ölümcül). **WeSpeaker'a
   geçildi** (`82dc3a0`). `experiments/speaker-encoder-ab/`.
4. **DÖNÜM NOKTASI**: encoder değişimi ayrımı ÇÖZMEDİ — Havva hâlâ Ayhan'a Ayhan'dan çok benziyordu.
   Kullanıcı: "ilk başta çalışıyordu, neden düştü?" → REGRESYON. Suçlu **auto-learn** (benzer sesi
   profile ekliyordu; sabah 109 kirli örnek, canlıda tekrar). **Sorun encoder değil, naif skorlama.**
5. Kullanıcı: "hazır sistem yok mu?" → araştırıldı: pyannote/SpeechBrain de aynı naif kosinüs+eşik.
   Profesyonel fark = **AS-norm/PLDA kalibrasyon**. Picovoice Eagle gerçek hazır SDK ama internet/
   AccessKey (off-grid çelişki).
6. **AS-norm kuruldu ve KANITLANDI** (`experiments/speaker-asnorm/`, cohort=fleurs tr_tr 120 konuşmacı):
   gerçek Ayhan+Havva sesinde marj +0.14 (ham) → +2.0 (AS-norm), çapraz-kişi NEGATİF → **ayırdı**.
   **Canlıya entegre edildi** (`5471f51`, `worker/speaker_id.py`). Güvenli geri-düşüş (cohort yoksa
   ham-kosinüs).
7. **FAR/FRR/EER kalibrasyon** (`calibrate.py`): AS-norm EER %5.1 vs ham %12 (okuma corpus).
8. **Kullanıcı gözlemi (kritik)**: okuma ≠ doğal konuşma. ÖLÇTÜM: okuma EER %5 ama **doğal EER %19**;
   okuma-eşiği (+1.864) doğal seste kullanıcıyı **%58 reddediyor**. → Okuma kalibrasyonu tehlikeli
   iyimser, eşiği okuma verisinden koyMA. `[[kalibrasyon-okuma-vs-dogal]]`.

### Multimodal (mmproj) CANLI BEYİNDE AÇILDI — akşam turu
Kullanıcı Gemma-4 mid-2026 patch görselini getirdi; incelendi → o düzeltmeler bizim stack'e (llama.cpp,
Ampere 3090) uygulanmıyor (patch'ler vLLM/Hopper). Ardından mmproj yüklendi:
- **`candan-brain.service` ExecStart'a `--mmproj mmproj-gemma-4-12b-it-qat-q4_0.gguf` eklendi**;
  test için `--chat-template-file gemma4-fixed.jinja` KALDIRILDI (gömülü native template). MTP korundu.
  Yedek: `/root/candan-brain.service.bak-mmproj`.
- **Görüntü ✅** (kırmızı→"Kırmızı"). **Ses ✅ ama erratic** (440Hz→"kısa ses"; yüksek temp "dosya yok"
  halüsinasyonu — `micro_services/gemma/transcribe.sh` uyarısı birebir). **Video ❌** (llama-server yok).
- **mmproj + MTP birlikte çalışıyor** (`draft_n:4 accepted:4`). VRAM ~9.6GB proses, ~7.8GB boş.
- **Kanıt kaynağı:** `/Users/drascom/work/micro_services/gemma` — aynı model+mmproj llama.cpp kurulumu.
  API: image=`image_url` data URI, ses=`input_audio {data,format}`.
- **Tool regresyonu ÖLÇÜLDÜ → YOK.** `bench/ab_bench.py tool_secim`+`kontrol` gömülü-template canlı beyne:
  reminder_add/memory_add/soul_add **20/20**, yanlış-tool **0/10** — `fixed.jinja` baseline'ıyla birebir.
  → Bu build'de (17a05e4) fix template gereksiz, birleşik-template turu GEREKMEZ. `[[gemma4-native-audio]]`.
- BEKLEYEN: kullanıcı görsel test yapacak (kendi gözüyle); pi/worker istemci tarafına image/audio
  content-part göndermeyi bağlamak ayrı iş.

---

## YAPILACAK (öncelik sırasıyla)

### 1. CANLI-AKIŞ HATALARI (asıl blokör — bunlar olmadan kayıt/tanıma canlıda çalışmaz)
Kullanıcı bu turda "canlı-akış hatalarını düzelt"i seçti — yeni oturumda BURADAN başla.
- **(a) Toplayıcı bazen 0 pencere yakalıyor** (`gördü=0`): kaotik oturumda kayıt reddedildi.
  Kök: muhtemelen `agent_busy` kapısı Candan sürekli konuşurken her pencereyi düşürüyor, ya da
  collection penceresi çok kısa. `pi_brain.py` `_collect_loop`/`_start_collect` timing'i incele.
- **(b) Model kaydı YALAN söylüyor**: reddedilmiş kayda "başarıyla kaydettim" diyor (worker'ın
  gerçek "Sesini tam alamadım"ının önüne kendi uydurmasını ekliyor). `[[soylemeden-once-yap]]` ailesi.
  Enrollment sonucunu SADECE worker söylemeli, model tekrar etmemeli/uydurmamalı.
- **(c) Tanıma modele görünmüyor** (kullanıcının önerdiği extension): "beni tanıyor musun" deyince
  model `memory_search` (notlar) bakıp "tanımıyorum" diyor — oysa speaker-tap onu tanıyor. ÇÖZÜM:
  worker tanıma sonucunu (`speaker_state.current`) modele ENJEKTE etsin + salt-okuma kayıt-sorgu
  extension. `[[speaker-tanima-modele-gorunur]]`. Model KARŞILAŞTIRMAZ (worker yapar), sonucu OKUR.

### 2. Eşik tuning (canlı-akış düzelince)
- AS-norm eşiği `-1.0` TAHMİN. Okuma corpus'u güvenilmez (iyimser), doğal corpus karışık (Ayhan
  LiveKit + Havva telefon, küçük). Ölçülen noktalar: okuma EER −0.23, doğal EER −1.95, doğal
  düşük-FRR −2.75.
- **Aile asistanında ev sahibini reddetmek > yabancıyı kabul etmek** → eşiği düşük-FRR tarafına eğ,
  canlıda SIKILAŞTIR. Gerçek eşik **canlı kullanımda** oturur (günlerce "seni tanımadı"/"karıştırdı" say).
- İstenirse: Havva'dan TEK temiz doğal kayıt (asistan mic, doğaçlama) → telefon-confound'unu kaldırır,
  daha iyi offline eşik. `calibrate.py --dir natural|bench`.

### 3. Daha sonra
- Kalibrasyon corpus'unu büyüt (farklı mesafe/gürültü/misafir/yabancı) → dış yorumun istediği
  gerçek FAR/FRR/EER. `[[dis-degerlendirme-dogrulama]]`.
- pi 0.80.6 → 0.80.10 yükseltme (ertelendi).
- Prewarm `last_speaker.json` hiç yazılmıyor (`pi_brain.py:~1468`) — 2. kişi kaydolunca sorun.
- STT halüsinasyonu "Çeviri ve Altyazı M.K." STT katmanında düşür (model soul.md'de yok saymak yerine).
- Gemma 4 native audio (STT'siz) — **mmproj CANLI beyinde AÇIK** (yukarı bak); ses erratic, Türkçe
  belirsiz → Whisper hibrit yedek kalsın, doğal Türkçe kalitesi hâlâ ölçülmedi.

---

## GOTCHA / KARARLAR (yeni oturumda bilmezsen tuzak)
- **Delegasyon = Claude Agent tool** (cmux değil, bu projede). Worker'lara net görev + "uydurma,
  emin olmadığını söyle" yaz — bugün worker'ların dürüst "doğrulamadım"ları gerçek hata yakaladı.
- **Mac .env ≠ Sunucu .env**: Mac'te encoder hâlâ campplus/192, SUNUCUDA WeSpeaker/256. Bir worker
  Mac'e bakıp "AS-norm çalışmaz" yanlış alarmı verdi. Speaker işlerinde SUNUCUYU kontrol et.
- **`worker/models/` ve `worker/data/` GİTIGNORE** → git deploy taşımaz. Model/cohort/db için `scp` şart.
- **ssh üzerinden python heredoc = tırnak kaçış cehennemi**. Scripti dosyaya yaz, `scp`, çalıştır.
- **Türkçe log + `cut`/`awk` = "illegal byte sequence"**. `export LC_ALL=en_US.UTF-8`.
- **Veri (db/hafıza/ses) SİLME → ARŞİVLE** (`~/candan-lite-arsiv/`). Bugün bu iki kez işe yaradı
  (0.753 kirlenme ölçümü arşivden çıktı).
- **Ben (Claude) bu turda birkaç kez bakmadan konuşup yanıldım** (AEC "kanıtlandı", rms→"mikrofon ölü",
  encoder odağı). Kullanıcının "ilk başta çalışıyordu" ve "neden sıfırdan yapıyoruz" soruları iki kez
  doğru yöne çevirdi. Speaker işlerinde ÖLÇ, tahmin etme.

## Bugün yazılan hafıza notları (yeni oturum bunları okusun)
mimari-harness-denetleyici, mimari-extension-once, wake-iskasi-vandam, kayit-testi-16-17,
speaker-encoder-secenekleri, hazir-speaker-id-sistemi, kalibrasyon-okuma-vs-dogal,
speaker-tanima-modele-gorunur, dis-degerlendirme-dogrulama, gemma4-native-audio,
hafiza-tek-kaynak-sunucu (güncel), planli-geriye-donuk-temizlik.

## Deneyler (hepsi `experiments/`, ses git dışı, kullanıcı BİR DAHA okumayacak)
- `speaker-encoder-ab/` — campplus vs WeSpeaker A/B (`ab.py`)
- `speaker-asnorm/` — AS-norm + cohort + `calibrate.py` (FAR/FRR/EER, `--dir bench|natural`).
  Corpus: `bench/` (okuma), `natural/` (doğal). İkiniz de aynı metni okudu + doğal kayıtlar mevcut.
