# DEVİR — 28 Temmuz 2026

Önceki devir: `handoff/2026-07-27-DEVIR.md` (o belgedeki uyarılar HÂLÂ GEÇERLİ, tekrar etmiyorum).

Bugünün konusu tek bir başlıkta toplanıyor: **ses kimliği güvenilir değil.** Yanında
warm-up gecikmesi çözüldü. Gün, ölçüm altyapısı kurularak kapandı — asıl tasarım işi
veri birikince yapılacak.

---

## 1. CANLIYA ALINANLAR (hepsi `.25`'te çalışıyor)

| İş | Commit | Etki |
|---|---|---|
| Warm-up: prewarm artık kuru prompt atıyor | `c3f9cb2` | İlk tur **21 sn → 3 sn** |
| `prewarm_fnc`: VAD/EOU/campplus job yolundan çıktı | `d24f21f` | ~2-2.7 sn |
| `TURN_MAX_SECONDS` tavanı kanıtı atmıyor, kırpıyor | `4942f11` | Uzun turlar artık tanınıyor |
| Sözlü onay oturumu bağlıyor (`current` set) | `314fd22` | "Evet ben Ayhan'ım" artık işe yarıyor |
| Gölge embedding kaydedici (salt gözlem) | `4a7109c` | Ölçüm altyapısı |
| Kimliksiz hafıza notu atılmıyor, beklemeye alınıyor | `5807004` | Sessiz veri kaybı bitti |
| Onay zehirlenmesi kapatıldı | `acbc23c` | "evet" artık dar kapıdan geçiyor |
| Tanıdığı hâlde inkâr bitti (`_identity_note` guard) | `cc93cd7` | |
| Yanlış aday yayınlanmıyor | `daa3af3` | Yanlış atfetme riski düştü |
| Bekleyen not cevabı İŞLENİYOR (`memory_attribute_pending`) | `af8b546` | "Kibar kayıp" bitti |
| Prozodi + akış alanları (npz şema 2) | `0ef40c9` | Akış analizi mümkün oldu |

Yedekler: `/opt/.deploy-backup-20260728-{1747-kimlik, 1909-kimlik2, 2123-prozodi}`
ve `speakers.db.bak-zehirlenme-20260728-1939`.

**478 test geçiyor** (gün başında 402). ⚠️ `worker/tests/` `.gitignore`'da — testler
diskte, depoda DEĞİL (kullanıcı kararı, `.gitignore:52`).

---

## 2. ÖLÇÜLEN GERÇEKLER (tahmin değil — bunlara güvenilebilir)

**Kimlik başarısızlığının sebebi eşik DEĞİL, tur uzunluğu.**

| Pencere sayısı (≈sn) | 0 | 1 | 2 | 3 | 5 | ≥7 |
|---|---|---|---|---|---|---|
| İsim verilebildi | %0 | %7 | %59 | %67 | %82 | %43 |

* 18-28 Tem arası **790 kararın 557'si (%70.5) Bilinmeyen**. Turların **%38'i** ölü bölgede (<2 pencere).
* Skor dağılımları örtüşmüyor: kabul medyanı **+4.17**, red medyanı **−3.71**, eşik −1.00.
  **Eşiğe DOKUNMA** — sorun orada değil.
* Aday verisi olan 82 Bilinmeyen kararın **46'sı (%56) oran=1.00** — çelişkisiz kanıt vardı, atıldı.
* ≥7'deki düşüş `TURN_MAX_SECONDS=8` tavanındandı (düzeltildi).

**Canlı model beklenenden farklı:** `wespeaker_en_voxceleb_resnet34_LM` (256d, **İngilizce
VoxCeleb**, ≥2-3 sn segment için eğitilmiş). campplus DEĞİL — lokal `.env` yanlış yazıyordu.
İlk penceremiz 1.0 sn, yani modeli tasarlanmadığı yerde çalıştırıyoruz.

**Ham kosinüs yanıltıcı:** Ayhan-Havi merkezleri arası ham kosinüs 0.672. Bu NORMAL —
embedding'ler büyük ortak bileşen taşır, AS-norm bunu temizliyor. Ham kosinüsle
"karıştırıyor" sonucu ÇIKARMA.

**Donanım:** 6 CPU çekirdeği boş (yük 0.02), 14 GB RAM. ⚠️ GPU VRAM **21.2/24 DOLU** —
ikinci büyük model SIĞMAZ. CPU tasarımı yap.

---

## 3. SIRADAKİ İŞ — veri birikince

Gölge kaydedici artık her pencere için embedding **ve** akış bilgisi yazıyor
(`worker/data/session-emb/<id>.npz`, şema 2). Birkaç günlük gerçek konuşma birikince
şunlar **canlıya hiç dokunmadan**, aynı veri üzerinde replay ile ölçülür:

1. **Komşu-karşılaştırma (kullanıcının fikri — ANA TASARIM).** Sabit referans yerine
   aynı oturumun yakın geçmişiyle karşılaştır; iki taraf aynı kanal/ortam/ruh halini
   paylaştığı için koşul farkı düşer. Kanıt: 16:25:05'te 15 pencere hepsi Ayhan (ort 5.69)
   iken hemen sonraki 6 tur tek pencereli diye reddedildi.
2. **Oturum kapsamlı kanıt biriktirme** — zaman-sönümlü toplam; çelişkide sıfırlanır.
3. **Prozodinin rolü:** sıfırdan kimlik ATAMASIN, **tek pencereli turları kurtarsın**
   (ikinci onay penceresinin yerine geçsin).
4. **Medyan/kırpılmış ortalama** — ortalama zehirlenmeye açık (bugün kanıtlandı).
5. **Merkezleme (centering)** — ortak bileşen temsil düzeyinde çıkarılmıyor, sadece
   skor düzeyinde (AS-norm).
6. ⚠️ **`confirmed-learn` ağırlığı:** `_AUTO_SOURCES = {"auto-learn"}` (`speaker_id.py:39`),
   yani onaydan öğrenilenler **tam kayıt ağırlığıyla** (0.7) merkezi çekiyor. Kırılganlık.

**Ölçüt çifti — pazarlık yok:** kaçırma oranı (bugün %70.5) **ve** yanlış atama oranı
BİRLİKTE raporlanacak. Yalnız biri düzelirse tasarım REDDEDİLİR. Etiketli veri yok,
bunu mutlak doğruluk gibi sunma.

**Ayrı duran işler:**
* `truth_check` — uydurup sonra "kontrol ettim" demesi. İki kanıt: Londra havası (16:25,
  arama cevaptan 12 sn SONRA koştu), elma kalorisi (18:05, "800 kalori" + hiç arama
  yapmadan "internetten kontrol ettim" dedi). DEVIR'de zaten "bozuk" kayıtlı.
* Model değişimi: **WeSpeaker VoxBlink2 SimAMResNet34** adayı (111k konuşmacı, çok dilli,
  aynı mimari sınıfı, ONNX hazır). ⚠️ Embedding boyutu **DOĞRULANMADI** — 256 değilse
  Ayhan ve Havi yeniden kaydolur.
* **DTW ölü** — wyoming kelime zaman damgası vermiyor, `wake_stt.py` sadece metinde
  "candan" arıyor, sherpa yalnız havuzlanmış vektör dönüyor. Bedava ikame: turun İLK
  penceresi ≈ uyandırma kelimesi, `t_rel` alanı bunu offline test ettirir.

---

## 4. KULLANICININ YAPACAKLARI (kod işi değil)

* **Havi'nin kaydını çeşitlendir.** 9 örneğinin 6'sı hâlâ tek oturumdan (22 Tem 15:27).
  Sektör bulgusu: örnek sayısından çok **oturum çeşitliliği** belirleyici (farklı gün,
  mesafe, ruh hali). Muhtemelen en yüksek getirili tek adım.
* **Konuş, veri biriksin.** Replay'in güvenilirliği doğrudan buna bağlı.
* **Görsel test kullanıcıya ait** — ajan uygulama açıp bakmaz.

---

## 5. BUGÜN BULUNAN TUZAKLAR (tekrarlamayın)

* ⚠️ **`pi` süreci `/opt/candan-lite`'ta koşuyor**, `pi-service`'in systemd
  `WorkingDirectory`'si (`/opt/candan-lite/worker`) DEĞİL. Broker onu farklı cwd ile
  başlatıyor. `memDir(cwd)` bu yüzden `/opt/candan-lite/memory` çözüyor.
  systemd ayarına bakıp yol çıkarmak YANILTIR — `/proc/<pid>/cwd`'ye bak.
* ⚠️ **Onaydan öğrenme ile kimlik bağlamayı AYIR.** 28 Tem 18:06:47'de bir yakınma
  cümlesi onay sayıldı ve Havi profiline 2 kirli örnek yazıldı (silindi). Zayıf kanıtla
  `speakers.db`'ye YAZMA.
* ⚠️ **Sessiz kaybı durdurmak yetmez.** Spool eklendi ama cevabı işleyen yol yoktu →
  "kibar kayıp" oldu (soruyor, cevap alıyor, yine kaydetmiyor). Bir akış eklerken
  **cevap yolunu da** kur.
* ⚠️ **Otokorelasyonda oktav tuzağı:** saf 200 Hz sinüs 66.7 Hz ölçüldü. El yazımı sinyal
  işleme koduna **bilinen-sinyal testi** yazmadan güvenme.
* ⚠️ **`index.ts` tsconfig kapsamında DEĞİL** (bilinçli). `check.sh` onu kontrol etmiyor;
  değiştirirsen elle tip kontrolünden geçir. `pending.ts` kapsama alındı.
* ⚠️ **`web/node_modules` yoksa `check.sh`'ın tsc adımı sessizce ATLANIR.** Kurulu tut.
* ⚠️ Paralel worker çalışırken **`git stash` KULLANMA** — diğerinin dosyalarını süpürür.

---

## 6. ARAŞTIRMA BELGELERİ (hepsi depoda)

* `arastirma-A-ses-kimligi-mevcut-durum.md` — ölçüm, skor dağılımı, süre-başarı tablosu
* `arastirma-B-ses-dogrulama-yontemleri.md` — kanıt biriktirme, kalibrasyon, kümeleme
* `arastirma-C-belirsizlik-altinda-hafiza.md` — atfetme tasarımı, spool+sor
* `arastirma-D-iki-katmanli-kimlik.md` — hızlı yol / arka plan yeniden değerlendirme
* `arastirma-E-biyometrik-ses-teknikleri.md` — model adayları, metne-bağlı doğrulama, QMF
* `arastirma-F-akis-temelli-dogrulama.md` — prozodi alan listesi, DTW fizibilitesi

---

## 7. ÇALIŞMA NOTLARI

* Depo **PUBLIC**. Embedding ve prozodi biyometrik veri sayılır — `worker/data/`
  `.gitignore`'da, orada kalsın. Ham ses saklanmıyor, saklamayın.
* Deploy disiplini: **yedek → gönder → md5 → restart → log**. Geri dönüşü TEK BLOK yaz.
* ⚠️ Restart sırası: `candan-worker`, `pi-service`'e `Requires=` ile bağlı.
  pi-service'i restart edersen worker'ı ELLE geri başlat. Yalnız worker değiştiyse
  pi-service'e DOKUNMA.
* `AGENTS.md` sadeleştirildi; **codebase-memory artık kod işlerinde ilk durak** —
  Grep/Glob ondan sonra gelir.
