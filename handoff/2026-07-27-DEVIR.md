# DEVİR — 27 Temmuz 2026, 14:40

Yeni oturum buradan devam etsin. Önce §1 (durum), sonra §5 (sıradaki plan).
Detay için: `2026-07-27-gece-raporu.md`, `2026-07-27-higgs-canliya-gecis.md`.

---

## 1. SİSTEM ŞU AN

```
higgs-tts          active    ← CANLI TTS (Higgs TTS 3, streaming açık) — TEK motor
omnivoice-bridge   YOK       ← 28 Tem'de kaldırıldı (§6), geri dönüş kolu yok
candan-worker      active
candan-brain       active    (llama-server, gemma-4-12b-qat)
pi-service         active    (broker)
GPU 21549 / 24576 MiB · 402 test geçiyor (vurgu `[emphasis]` CANLIDA 28 Tem 00:53 — §3j)
```

**Ölçülen gecikme (sabahtan bugüne):**

| | sabah | şimdi |
|---|---|---|
| beyin ilk token | ~5.0 s | **1.2 s** |
| TTS ilk ses (uzun cevap) | 6.2 s | **0.55 s** |

Kullanıcı canlıda doğruladı: *"Gayet akıcı konuştuk. Ses tanımayı denedik. Eklediğimiz
5 duyguyu kullandı."*

⚠️ **`candan-worker`, `pi-service`'e `Requires=` ile bağlı.** `systemctl stop pi-service`
worker'ı DA durdurur ve pi-service'i başlatmak worker'ı geri GETİRMEZ. 27 Tem 14:31'de
bu yüzden asistan 9 dk kapalı kaldı. pi-service'e dokunduysan worker'ı ELLE başlat.

---

## 2. YAPILANLAR (26-27 Temmuz)

### TTS: OmniVoice → Higgs TTS 3
`bosonai/higgs-tts-3-4b` (4B, BF16 9.31 GB). Kullanıcı sesini beğendi ("çok doğal").

- **Neden:** Türkçe. Higgs `%25`, `3.500 lira`, `14:30'da`, `1994'te`'yi HAM METİNDEN
  doğru okuyor. WER 0.085 → **0.028**. Bedeli hız: RTF 0.305 → 0.50.
- **Streaming** (`POST /api/tts/stream`, chunked PCM): ilk ses 6.2 s → 0.55 s.
  ⚠️ Kodek çözücüsü SAĞA bakıyor — blok sağından 8 kare atılıyor, solundan 16 kare
  bağlam. Bu olmadan cızırtı oluyor. **Bu yapıyı bozma.**
- **Duygu ÇALIŞIYOR** (OmniVoice'ta `instruct` ölüydü, sadece hız vardı):
  `[mood:excited]→<|emotion:enthusiasm|>` · `[mood:sad]→<|emotion:sadness|>` ·
  `[laughter]→<|sfx:laughter|>Haha,` · `[sigh]→<|sfx:sigh|>Haah,` ·
  `[surprise-oh]→<|emotion:awe|>` (15:11'de `surprise`'dan değişti — §3'e bak)
- ⚠️ **Ölçülmemiş token canlıya alınmaz** — ve ölçüm **CANLI YOLDAN** yapılmalı.
  (`elation` bir süre "bozuk" sanıldı; deney koşumundaki ölçüm yanıltmıştı, canlı yoldan
  24/24 temiz çıktı. Bkz. §3.)
- ~~Higgs ve OmniVoice **aynı anda çalışamaz** (systemd `Conflicts=`).~~ Konu kapandı:
  OmniVoice 28 Tem'de kaldırıldı, `Conflicts=` satırı da unit'ten çıktı (§6).

### Dört ayrı hafıza hatası — hiçbiri konuşmacı tanımayla ilgili değildi
| # | Hata | Çözüm |
|---|---|---|
| 1 | Dev modda hafıza tamamen kapalı | dev personasına kendi kökü (`memory/personas/dev/`) |
| 2 | **Normal modda da kapalı** — ortak odada `MEM_USER` süreç ömürlü | kimlik tur ömürlü yapıldı (atomik dosya + TTL) |
| 3 | Model hafıza talimatını hiç görmüyordu | ortak odada `MEMORY_NOTE` boot'ta veriliyor |
| 4 | Araç "kaydedilmedi" derken model "kaydettim" diyordu | `truth_check` + AGENTS.md kuralı |

### Harness doğrulama katmanı (`worker/truth_check.py`)
İlke: **model NE YAPILACAĞINA karar verir, harness NE OLDUĞUNU söyler.**
- Katman 1: tur defteri (`toolResult` + `isError`; `memory_add` guest'te `isError`
  OLMADAN "kaydedilmedi." döner → metin işaretleri de okunur)
- Katman 2: kritik yazma hata dönerse **modelin cümlesi canlıya ÇIKMAZ**, harness
  deterministik cümleyi söyler. Mod çelişkisi de deterministik.
- Katman 3: sınıflandırıcı (84-130 ms) — YALNIZ deterministik katmanlar yakalamazsa
  ve turda hata varsa. Tipik turda hiç çağrılmaz (testle kanıtlı).

**Ölçüldü:** AGENTS.md araç-gerçeği kuralı → hatayı kullanıcıya söyleme **0/10 → 6/6**
(12B tabanı `bench/12b/sonuclar.md` §4 ile, aynı model).

### Diğer
- Konuşmacı tanıma: VAD yeniden tetiklendiğinde kanıt tamponu siliniyordu → tur sınırı
  final transkript. "Güvenli pencere yok" hatası 30 → 0.
- Turn-detector: yerel çok dilli EOU modeli (CPU, ~20-100 ms). Cümle ortası duraklamada
  tur artık bölünmüyor.
- Compaction arka plana alındı + kaybolan soru yeniden gönderiliyor.
- Mod anahtarı ters çalışıyordu ("çık" deyince giriyordu) → düzeltildi.
- `pi/personas/ayhan.md` kaldırıldı — Ayhan tanındığında `candan.md` HİÇ yüklenmiyordu.
- **Oturum sıfırlandı** (27 Tem 14:31): 595 KB geçmiş → taze. Beyin 4× hızlandı.
  Eski geçmiş silinmedi: `sessions/...candan.jsonl` id=`candan-eski-20260727T143100`
  + `.bak-reset-20260727T143100`.

---

## 3. DUYGU KATMANI — BİTTİ (14:45, commit `d1faa2a`)

43 token ölçüldü (token başına 12 örnek, sınırdakiler 24, **canlı streaming yolundan**,
Whisper geri-dönüşü). **37 temiz / 6 şüpheli / 0 tamamen bozuk.** 21 emotion'ın hepsi
12/12, WER 0.000. Tam tablo: `handoff/2026-07-27-duygu-katmani.md` §2.

**⚠️ ÖNEMLİ DÜZELTME: koddaki "elation bozuk" kaydı YANLIŞMIŞ.** Canlı yoldan 24/24 temiz.
Eski ölçüm DENEY koşumundan alınmıştı, canlı yoldan (referans klonu + streaming) değil.
`excited` yine `enthusiasm`'da bırakıldı — ikisi de temiz, ölçülmüş kazanç olmadan canlı
davranış değişmez. **Ders güncellendi: ölçüm CANLI YOLDAN yapılmalı, deney koşumu yanıltır.**

**`pause` / `long_pause` çalışıyor ama yerleşim kritik — iki kural ölçüldü:**
1. Token iki yanında **boşluk olmadan** durmalı (boşluklu 12'de 3 kez ilk kelimeyi yedi,
   boşluksuz 0/12) → `_HUG_INLINE_RE`
2. **Cümle başına yakın duraklama zehirli** (2. kelimeden sonra 24'te 2 kez "Bir"i yuttu);
   aynı token cümle ortasında 24/24 temiz → `_MIN_WORDS_BEFORE_PAUSE = 3`
   Gerçek sessizlik: `pause` +0.32 s, `long_pause` +0.50 s.

**Eşlemeye eklenenler:** `[pause]`, `[long_pause]`, `[mood:warm]`→affection,
`[mood:calm]`→contentment, `[mood:proud]`→pride, `[mood:confused]`→confusion.
Temiz çıkan 37'nin hepsi ALINMADI: `anger/disgust/fear/shame` ev asistanına uymuyor,
`speed_*`/`pitch_*` etkisi zayıf (|Δsüre| ≤ 0.2 s). `[question-*]` hâlâ siliniyor —
`pitch_high` temiz ama "soru tonu" demek değil, uydurma eşleme yapılmadı.

**Anlatılan-etiket düzeltildi:** sağ bağlamla ayırt ediliyor (etiketten sonra
`gibi/diye/etiketi` gelirse ya da tırnak içindeyse okunur karşılığına çevriliyor:
"şaşırma efekti"). Gerçek kullanım etkilenmiyor, regresyon testiyle kilitli.

Prompt maliyeti: `pi/AGENTS.md` +8, `pi/personas/candan.md` +4 satır. **243 test OK.**
Deploy edildi ve servisler tazelendi (14:43). Yedekler: `*.bak-duygu-20260727-1436`.

⚠️ **Prompt motora BAĞLI:** `pi/AGENTS.md` + `pi/personas/candan.md` Higgs'e özgü
etiketler ürettiriyor. Motor bir gün değişirse (Piper) bu iki dosya da güncellenmeli,
yoksa Candan "pause" der. Bkz. `handoff/2026-07-27-duygu-katmani.md` §0.
(OmniVoice'a dönüş maddesi düştü — motor 28 Tem'de sistemden kaldırıldı, §6.)

### Kulak testi YAPILDI — şaşırma düzeltildi (15:11)

Kullanıcı `demo.html`'i ve ardından `surprise.html`'i (16 wav, iki cümle × 8 aday)
dinledi. **Tek düzeltme gerekti: `[surprise-*]` artık `<|emotion:surprise|>` değil
`<|emotion:awe|>`.**

`surprise` ölçümde temizdi ama kulakta şaşkın DUYULMUYORDU — ölçüm de bunu ima
ediyormuş: Δsüre **-0.02 s**, 21 emotion içinde en düşük, yani tabandan farksız.
`awe` ünlemsiz zor cümlede kullanıcı tarafından seçildi ve zaten ölçülüydü
(12/12, WER 0.000, Δsüre +0.35 s). `surprise+expressive_high` kombosu ünlemli
cümlede beğenildi ama **ölçülmediği için ALINMADI**.

Etiket adları, `pi/AGENTS.md`, `pi/personas/candan.md` DEĞİŞMEDİ — prompt maliyeti
aynı. Detay: `handoff/2026-07-27-duygu-katmani.md` §7b. Deney:
`experiments/higgs-tts3/surprise_set.py` · `./serve.sh surprise.html`.

**Ders (tekrar):** ölçüm "anlaşılıyor mu"yu söyler, "doğru duyuluyor mu"yu söylemez.
Δsüre ≈ 0 olan bir duygu token'ı büyük ihtimalle HİÇBİR ŞEY yapmıyordur.

Kalan kulak işi (acil değil): `pause`, `mood:warm`, `mood:proud` doğru duyuluyor mu.
```
cd experiments/higgs-tts3 && ./serve.sh demo.html
```

---

## 3b. KİMLİK ONAY DÖNGÜSÜ — CANLIDA (17:53, commit `fc1284c`)

Detay: `handoff/2026-07-27-kimlik-onay-dongusu.md` (deploy + geri dönüş komutları orada).

- **Kök sebep bulundu:** kayıt akışı 22 Temmuz'dan beri hiç çalışmıyordu
  (`kayıt havuzu BOŞ (gördü=0) → enroll REDDEDİLDİ`). Toplama latch'i `current is None`
  şartına bağlıydı; profili olmayan üçüncü kişi (Çiğdem) en yakın profile (Havi)
  yapışınca `current` doluyor ve latch hiç açılmıyordu → **yanlış tanınan kişi
  kaydolamıyordu.** Latch artık açık kayıt isteğinde `current`ten bağımsız açılıyor.
- **truth_check artık enroll'ü de kapsıyor:** kayıt tool'u sinyali görüldüğü andan
  itibaren modelin anlatısı bastırılır; reddedilmiş kayda "kaydettim" denirse harness
  deterministik düzeltmeyi söyler. ("Harika Çiğdem, ses kaydını aldım" vakası.)
- **Belirsizken SOR, onaylanırsa ÖĞREN:** aday hesaplanır (kimlik DEĞİL — persona swap'i
  ve kesin kimlik enjeksiyonu belirsizlikte hâlâ YOK), oran ≥ 0.6 + ≥ 2 güncel pencere +
  **10 dk soğuma** tutarsa tek deterministik soru sorulur. "Evet" → en çok 3 örnek
  `source='confirmed-learn'`. "Hayır" → hiçbir şey yazılmaz + bir kez tanışma teklifi.
- **Eşiğe DOKUNULMADI.** Gölge ölçüm log'u (-0.5/0.0/+0.5) + onay JSONL'i eklendi;
  asnorm eşiği bir sonraki turda VERİYLE seçilecek.
- Bayrak: `SPEAKER_CONFIRM_ASK_ENABLED` (false = bugünkü davranış, bire bir).
- **271 test geçiyor** (243 → +28). Deploy 17:53: yedekler `*.bak-onay-20260727`,
  traceback yok, worker `registered`, md5 5/5 eşleşti, yalnız `candan-worker` restart.
- **Canlı kanıt beklenen:** Çiğdem'i kaydederken Çiğdem'in KENDİSİ konuşmalı; log'da
  `gördü=` değerinin 0'dan büyük çıkması kök sebep düzeltmesinin kanıtıdır.

---

### 3f. KONUŞMA HIZI — gerçek bir kol (20:30)

Detay: `handoff/2026-07-27-konusma-hizi.md` (ölçüm tablosu + geri dönüş orada).

- **Canlı hata:** kullanıcı üç kez "biraz daha hızlı konuş" dedi, Candan üç kez
  "hızımı artırıyorum" (hatta "iki BİRİM daha") dedi, **tempo hiç değişmedi** —
  hız kolu YOKTU. Araç yalanının kardeşi: yetenek yalanı.
- **ÖLÇÜLDÜ, sonra seçildi** (koşul başına 12 örnek, canlı streaming ucu, Whisper):
  Higgs `<|prosody:speed_fast|>` en fazla **+%7.2** ve WER'i 0.004 → 0.030 yapıyor →
  **RED**. Motorun `speed` gövde parametresi **yok** (docstring yazıyor, kod hiç
  okumuyor; canlı da doğrulandı). **WSOLA tempo** (perde KORUNARAK, `worker/tempo.py`)
  **+%14.8 / +%29.7** kelime/s ve WER **tabanla aynı** (0.004) → **SEÇİLDİ**.
- **Dört kademe:** `[speed:slow]` · normal (işaretsiz) · `[speed:fast]` ·
  `[speed:very_fast]`. MUTLAK ve clamp'li — "iki birim" uydurmasının panzehiri.
  **Oturum ömürlü**: `reset_mood()` hıza DOKUNMAZ (asıl şikâyet buydu).
- **Streaming'e dokunulmadı**, filtre onun ÇIKIŞINDA: ilk ses **517 ms → 517 ms**.
  Perde korunuyor (F0 200.0 → 200.0 Hz, teste bağlandı).
- **truth_check katman 2'ye hız eklendi** (LLM çağrısı SIFIR): işaret koymadan ya da
  TAVANDAYKEN "hızlandırıyorum" dersen harness doğrusunu söyler.
- **Yetenek tarifi gerçeğe uyduruldu:** `[question-en]`/`[confirmation-en]` prompt'tan
  ÇIKTI (eşlemede siliniyorlardı — "onaylama" yalanının kaynağı), `[mood:warm|calm|
  proud|confused]` + `[speed:X]` girdi. Prompt net **+11 satır**.
- Bayrak: `SPEECH_SPEED=0` → işaret yine silinir, tempo uygulanmaz, ses bugünküyle
  bire bir aynı. **301 test geçiyor** (271 → +30). Deploy 20:30: yedekler
  `*.bak-hiz-20260727`, md5 **8/8** eşleşti, traceback yok, yalnız `candan-worker`
  restart (`higgs-tts` ve `pi-service`'e DOKUNULMADI).
- **Kullanıcıdan bekleniyor (kulak):** `cd experiments/konusma-hizi && ./serve.sh`
  → `http://localhost:8011/hiz-seti.html`. Hangi kademe "biraz daha hızlı" demek?
  Tavan `very_fast`=1.30; 1.45 ölçülü ve temiz, istenirse tek satırla kademe olur.

---

### 3g. SÖZÜNÜ KESME — yeni komut mu, sadece sohbet mi? (CANLIDA, 21:16)

Detay: `handoff/2026-07-27-sozunu-kesme.md` (geri dönüş komutları orada, tek blok).

- **Canlı hata (18:37):** Candan duygu örneklerini sayarken kullanıcı "Beni duydun
  mu?" dedi; cevap kesildi ve Candan *"az önceki örnekleri dinledin sanırım"* diye
  YANLIŞ varsaydı — kullanıcı hiçbirini duymamıştı. İki kusur: kesilen cevap ölüyordu
  ve model kesilen metnin DUYULDUĞUNU sanıyordu.
- **Kullanıcının iki kararı:** (1) sohbet sayılırsa **kaldığı CÜMLENİN BAŞINDAN**
  devam — metin YENİDEN ÜRETİLMEZ, elde olan metin kırpılır; (2) **şüphede YENİ
  KOMUT say, sussun** (telafisi "devam et" demek).
- **Sıra (truth_check MALİYET KAPISI deseninin aynısı):** geri-bildirim sözlüğü →
  yeni-istek işaretleri (soru/emir) → ≤2 kelime → küçük LLM (katman-3'ün **AYNI**
  ucu) → kararsızsa yeni istek. **Ölçüldü:** deterministik kapı medyan **2.5 µs**,
  devam metni **60 µs**; sohbet kararında pi'ya HİÇ gidilmez (0 token). Tipik turda
  sınıflandırıcı çağrılmıyor — testle kanıtlı.
- **Geçmiş dürüstlüğü:** kesilen metin geçmişe "söylenmiş" gibi girmiyor; bir
  sonraki prompt'a *kullanıcı YALNIZCA şu kadarını duydu* notu iliştiriliyor.
  "Ne söylendi" TAHMİN değil — livekit `conversation_item_added`'ın `forwarded_text`i.
- Bekleyen devamın ömrü **30 sn** (env). Kimlik/kayıt/sıfırlama akışları her zaman
  öncelikli; uykuda ve proaktif seslenme sırasında devam YOK.
- Bayrak: `BARGE_RESUME_ENABLED=false` → davranış bugünküyle bire bir.
  **339 test geçiyor** (301 → +38). `./check.sh` yeni bulgu üretmiyor.
- ⚠️ **Deploy sırasında bulunan tuzak:** `.env`, `agent.py`'nin `load_dotenv()`'iyle
  **import'lardan SONRA** yükleniyor (systemd `EnvironmentFile=` kullanmıyor) →
  modül seviyesinde env okuyan dosyalar `.env`'i GÖRMÜYOR. Ölçüldü: `BARGE_RESUME_
  ENABLED=false` yazılıyken modül değeri True kalıyordu, **geri dönüş kolu
  çalışmıyordu.** `barge.reload_settings()` ile düzeltildi (kapsam DAR: yalnız
  barge). **Aynı tuzak `truth_check`'in `CLAIM_CHECK_*` ayarlarında da var —
  dokunulmadı, ayrı iş** (bkz. §4 madde 7).
- Deploy 21:16: yedekler `*.bak-kesme-20260727` (+ `.env.bak-kesme-20260727`),
  md5 **3/3** eşleşti, traceback 0, worker `registered`, yalnız `candan-worker`
  restart — `higgs-tts` (13:45:56) ve `pi-service` (14:43:43) damgaları değişmedi.
- **Kullanıcıdan bekleniyor:** canlı deneme (5 adım, o notun §7'si).

---

### 3h. KOMBO ÖLÇÜMÜ — 10 koşulun onu da TEMİZ (21:5x)

Detay: `handoff/2026-07-27-kombo-olcumu.md`. **Canlıya HİÇBİR şey uygulanmadı**
(`worker/` açılmadı, sunucuya değişiklik yok, `higgs-tts` restart edilmedi) — bu bir
ölçüm turu; eşleme değişikliği ÖNERİ hâlinde bekliyor.

- Atlasta kulakla beğenilen 4 kombo canlı yoldan (`POST /api/tts/stream`) ölçüldü:
  kombo başına 12 örnek, Whisper geri-dönüşü, 43 token'lıkla **aynı dört ölçüt**.
  **14 koşul / 168 wav, hepsi TEMİZ** — 12/12 anlaşıldı, boş yok, **cümle başı yeme
  yok** (iki etiket yan yana gelince en çok korkulan kusur buydu).
- **Token sırası fark etmiyor:** beş kombonun ikisi sırası da ölçüldü, hepsi TEMİZ;
  süre farkı koşulun kendi standart sapmasından küçük. Tutarlılık için
  `<|emotion:X|><|prosody:Y|>` (emotion önce) öneriliyor.
- **Öneri:** `[surprise-*]` → `surprise+expressive_high` (bugün konan tekil `awe`
  kulakta kötüydü), `[mood:proud]` → `pride+expressive_high`, `[mood:calm]` →
  `contentment+expressive_low`; yedek `awe+expressive_high`.
- ✅ **KULAK TESTİ YAPILDI (20:55) — beşinin beşi de "komboyu al".** Kullanıcı:
  *"kesinlikle kombo daha güzel"* (pride), *"hem kombo hem yalnız surprise gayet
  güzel"* (ünlemsiz cümle). Öneri tablosu onaylandı; **uygulama sonraki tur.**
- ⚠️ **Δsüre işareti YANILDI — ders.** `surprise+expressive_high` ünlemsiz cümlede
  on koşulun en zayıf Δsüre'siydi (+0.09 s) ve "zayıf olabilir" diye işaretlenmişti;
  kulakta "gayet güzel" çıktı. §3'teki *"Δsüre ≈ 0 → token hiçbir şey yapmıyordur"*
  sezgisi **kombolar için geçerli değil**; `expressive_high` süreyi uzatmadan tonu
  değiştirebiliyor. Δsüre'yi eleme ölçütü yapmama kararı kendini doğruladı.
- 💡 **Yeni fikir:** kullanıcı `expressive_high`'ı tek başına da beğendi
  (*"farklı durumlarda üçünü de ayrı ayrı kullanabiliriz"*) — duygu değil **vurgu**
  kolu; duygudan bağımsız bir etikete (ör. `[emphasis]`) bağlanabilir. Ölçümü
  zaten TEMİZ (27 Tem, 12/12). Karar verilmedi.
- **SIRADAKİ İŞ (ajan):** `worker/higgs_tts.py` eşlemesinde üç satır —
  `[surprise-*]` → `surprise+expressive_high`, `[mood:proud]` →
  `pride+expressive_high`, `[mood:calm]` → `contentment+expressive_low`; sıra
  `<|emotion|><|prosody|>`. Kulak seti tekrar dinlenmek istenirse:
  `cd experiments/higgs-tts3 && ./serve.sh kombo.html` (8009).
  ✅ **UYGULANDI — bkz. §3i.**

### 3i. YENİ DUYGULAR + KOMBO UYGULAMASI — CANLIDA (22:08)

Detay: `handoff/2026-07-27-yeni-duygular.md`. **367 test OK** (357 → +10).
Prompt maliyeti net **+3 satır** (`AGENTS.md` +2, `candan.md` +1) — mood listesi
sıkıştırıldı, `candan.md`'deki tekrar eden yerleşim satırı silindi.

- **Beş yeni etiket:** `[mood:amused]`→amusement · `[mood:thinking]`→contemplation ·
  `[mood:determined]`→determination · `[mood:relieved]`→relief · `[whisper]`→
  `<|style:whispering|>` (kullanıcı: "harika"). Hepsi 12/12, WER 0.000.
- **`[whisper]` `[mood:]` altına KONMADI:** biçem (`style`), duygu (`emotion`) değil;
  `[laughter]`/`[pause]` gibi çıplak etiket deseni zaten yerleşik. ⚠️ Kapsamı
  **cümle** (mood turluk); prompt "fısıldanacak her cümlenin başına" diyor.
- **Üç kombo yükseltmesi uygulandı:** `[surprise-*]` → `surprise+expressive_high`,
  `[mood:proud]` → `pride+expressive_high`, `[mood:calm]` → `contentment+expressive_low`.
  Sıra `<|emotion:X|><|prosody:Y|>`.
- ⚠️ **`awe` eşlemeden TAMAMEN ÇIKTI — geri getirmeyin.** Ölçümü kusursuzdu ama
  kullanıcı kulaklıkla dinleyince tekil `awe`'yi KÖTÜ buldu (*"şuh kalmış, heyecan
  yok"*). Koda not düşüldü, `test_awe_is_gone_for_good` kilitliyor.
- **Kombo ATOMİK:** parçalarından biri dolu kategoriye düşerse tamamı atılır —
  yoksa `[mood:sad] … [surprise-oh]` cümlesinde `expressive_high` tek başına kalıp
  üzgün cümleyi canlandırırdı.
- 🔒 **KALICI DERS: "TEMİZ" = anlaşılır; Candan'ın SESİ demek DEĞİL.**
  `prosody:pitch_low` ("taban kadın sesi, etiketli erkek ses") ve `style:shouting`
  ("ses başkasına ait gibi") ölçümde tertemizdi ama konuşanın KİMLİĞİNİ değiştiriyor
  — asla kullanılmayacak. `speed_*` ise WSOLA lehine ölü yol. Üçü de koda yorum
  olarak yazıldı ve `test_identity_breaking_tokens_are_never_mapped` ile kilitlendi.
- Deploy: yedek `*.bak-duygu2-20260727`, md5 **3/3** eşleşti, traceback yok,
  `registered worker` 22:08:42, **yalnız `candan-worker` restart**
  (`pi-service` 14:43 / `higgs-tts` 13:45 damgaları değişmedi, doğrulandı).
  TTS cache SİLİNDİ (11 dosya): anahtar mood ADInı içeriyor, token değişti →
  bayat HIT tam da elenen sesi çalardı. Hız katmanı cache'e karışmıyor (ham PCM saklanıyor).
- **SIRADAKİ İŞ (kullanıcı):** kulakla canlı dinleme — kombolar tekilinden farklı
  duyuluyor mu, `[whisper]` çok cümleli yanıtta her cümlenin başına konuyor mu.

### 3j. VURGU — `[emphasis]` eşlemede, CANLIDA (28 Tem 00:53)

Detay: `handoff/2026-07-28-vurgu-esleme.md`. **402 test OK** (378 → +24).
Prompt maliyeti net **0 satır** (`AGENTS.md` 147→147, `candan.md` 38→38).

- **`[emphasis]` hedef kelimenin TAM ÖNÜNE.** Higgs TOKEN'ı DEĞİL: katalogda kelime
  vurgusu yok (uydurma `<|prosody:emphasis|>` harfi harfine okunuyor), o yüzden
  ölçülmüş bir NOKTALAMA yerleşimine çevriliyor — soldaki boşluk+virgül atılır,
  yerine `" — "` gelir, **arkaya hiçbir şey konmaz**:
  `Hadi kalk bakalım, [emphasis] on dakikaya…` → `Hadi kalk bakalım — on dakikaya…`
  Dizge deneydeki `tire-on` metniyle BİREBİR (test `vurgu_set.py`'yi çalıştırıp
  karşılaştırıyor). Ad İngilizce çünkü koddaki bütün işaretler öyle.
- ⚠️ **Arkadaki işaret bir daha ASLA:** iki yanlı tire hedeften sonra +0.31 s
  fazladan bekleme üretiyordu (kullanıcının altı ayrı notu). `test_nothing_is_added…`
- ⚠️ **Geriye doğru öbek kapsama (`bagli`) canlıya TAŞINMADI.** `awe` cümlesinde
  sınırı "hem de"nin önüne almak ÖLÇÜMDE üstündü (+2.17 vs −0.72) ama kulak
  **0/3** verdi, sade yerleşim **3/3**. Ölçüm bu deneyde üçüncü kez yanıldı.
- 🔒 **KABUL EDİLEN GERÇEK: vurgu cümlelerin ~YARISINDA tutuyor** (kulak: 2/4
  cümlede 3/3, 2/4'ünde 0/3). Kullanıcı bilerek seçti — başarısızlık ZARARSIZ:
  ses bozulmuyor, yalnız vurgu gelmiyor (WER 0.000, baş yeme 0, bekleme yok).
  "Neden hep çalışmıyor" sorusunun cevabı budur.
- Koruma: cümlede **en fazla bir** vurgu (ilki kalır) · cümle başındaki işaret
  ATILIR (`_MIN_WORDS_BEFORE_EMPHASIS = 1`; `[pause]`in 3 kelime kuralı AYNEN
  uygulanmadı — ölçüm 1 kelimeden sonra baş yeme 0 diyor) · sağında kelime yoksa
  atılır · düşen işaretin virgülü yerinde kalır · anlatılırsa "vurgu" okunur.
- Deploy: yedek `/opt/candan-lite/.deploy-backup-20260728-vurgu/`, md5 **3/3**,
  traceback yok, **yalnız `candan-worker` restart** + `pi-service` **reload**
  (prompt değişti; reload olmasa sıcak `pi` bayat prompt'la yaşardı).
  `higgs-tts` / `candan-brain` ELLENMEDİ.

---

## 4. AÇIK KALANLAR

1. **Kalabalık ortamda çalışmıyor.** Mikrofon tüm odayı alıyor, her konuşma turu kesiyor.
   Yapısal — eşik ayarıyla çözülmez. Seçenekler: wake kelimesi (`WAKE_ENABLED=true`),
   konuşmacı kapısı (tanımayı etiket değil GİRİŞ FİLTRESİ yapmak), kulaklık.
   ⚠️ Wake açılırsa: beyin *"(Candan kelimesi geçmediği için yanıt vermiyor.)"* metnini
   üretip SESLİ okuyor (26 Tem'de 12 kez). Wake'ten önce o düzeltilmeli.
2. **Compaction beklemesi** — sıkıştırma sürerken yeni tur sıraya giriyor (tek agent
   oturumu). Sessizlik çözüldü, BEKLEME çözülmedi. Oturum sıfırlandığı için bir süre
   tetiklenmeyecek.
   **ÖLÇÜLDÜ (27 Tem 23:xx, `handoff/2026-07-27-gecikme-ve-stall.md`):** bu madde
   sanıldığından KÜÇÜK. 22:05-22:27 penceresindeki 8 tur stall'ının yalnız **1'i**
   compaction penceresine denk geliyor; **6'sı** sözünü-kesme sınıflandırıcısının
   (`barge.classify_llm`) tek slotlu `candan-brain`'e attığı 83-142 token'lık
   yabancı istekten, 1'i de onun zincirinden. O istek 15-29 bin token'lık konuşma
   KV önbelleğini slottan atıyor → sıradaki tur bağlamı baştan işliyor (13-15 s,
   ~1120 token/s ölçüldü) → 12 s'lik watchdog SAĞLIKLI turu öldürüyordu.
   Compaction'ın kendisi işini yapıyor: bağlam 28 787 → 15 305 ve 29 646 → 15 043
   token (≈ **−%48**). ✅ **Watchdog düzeltildi** (prefill farkındalığı: llama-server
   `GET /slots` → `is_processing`/`n_prompt_tokens_processed`/`n_decoded` ilerliyorsa
   tur öldürülmez; tavan `PI_PREFILL_GRACE=60`, geri dönüş `=0`). Beklemenin KENDİSİ
   duruyor: kalıcı çözüm `candan-brain --parallel 2` — **sunucu ayarı, kullanıcıya
   sorulacak** (slot başına bağlam 65 536 → 32 768'e iner, ölçülen tepe 29 646).
   ✅ **KÖK SEBEP DE KESİLDİ (27 Tem 23:08, kullanıcı kararı):** `.env`'de
   `BARGE_CHECK_ENABLED=false`. Sınıflandırıcının LLM kademesi susturuldu → 8
   stall'ın 7'sinin kaynağı olan yabancı istek artık beyne HİÇ gitmiyor
   (ölçüldü: kapalıyken beyne giden istek **0**, açıkken **1**). Deterministik
   kapı (kelime sözlüğü, soru/emir işaretleri) çalışmaya devam ediyor; kararsız
   kalınca "yeni komut say, sus" — zaten varsayılan davranış. `candan-brain`'e
   ve `--parallel`'e DOKUNULMADI. Geri dönüş: `.env`'de `true` yap + worker restart.
   ✅ **BEKLEME ARTIK GÖRÜNÜR VE KAPALI** (28 Tem, `2026-07-28-compaction-haber-ver.md`).
   **Sıkıştırmanın süresi İLK KEZ ÖLÇÜLDÜ** — log'da bitiş satırı bile yoktu:
   `candan-brain` `print_timing` ile 7 sıkıştırma: 20.7 / 21.2 / 23.3 / 23.6 / 33.3 /
   35.2 / **61.5** sn → **ortanca 23.6 sn**. Yani bu, saniyelik bir duraklama değil
   yarım dakikalık bir KESİNTİ. Kullanıcının isteğiyle: pencere başlarken
   deterministik cümle ("Hafızamı toparlıyorum, yarım dakika kadar sürebilir…"),
   pencerede gelen tur pi'ya GİTMEZ (kısa işaret: "Bir saniye, hâlâ toparlanıyorum."),
   bitişte "Tamam, hazırım.". Sıkıştırma yine ARKA PLANDA (senkron yapmak §2'deki
   sessizlik hatasını geri getirir). Eşik `PI_COMPACT_NOTIFY_MIN_S=2` altındaki
   sıkıştırma sessizce geçer. Kaybolan-soru mekanizması **KORUNDU**: kapı yalnız
   sıkıştırma BAŞLADIKTAN SONRA gelen turu reddeder; öncesinde sorulup yutulan soru
   yine bir kez yeniden gönderilir (o turda "hazırım" denmez). Geri dönüş tek satır:
   `PI_COMPACT_GATE_ENABLED=false`. Kalan iş hâlâ aynı: beklemeyi gerçekten
   PARALELLEŞTİRMEK (§5 madde 6) ve `--parallel 2` kararı.
3. **`"ekledim"` yanlış-pozitifi** — truth_check kelime listesinde.
   ✅ **CANLI ÖRNEĞİ ÇIKTI ve DÜZELTİLDİ** (27 Tem 22:25, Aslı denemesi): model
   kayıt sihirbazının TALİMATINI verirken ("Aslı, seni not aldım. Şimdi ses
   kaydınızı alacağım…") arkasına haksız "Aslında bunu kaydetmedim" eklendi.
   Çözüm: kayıt iddiaları YUMUŞAK/GÜÇLÜ diye ayrıldı; yumuşak kalıp + turda hiç
   yazma aracı yoksa düzeltme EKLENMİYOR. Ayrıntı: `2026-07-28-kayit-sirasi.md` §B.
   Kilit test: `python worker/truth_check.py` (vaka (f)). ⚠️ Katman 2 (memory_add
   guest → `isError`SİZ "kaydedilmedi") aynen duruyor; vaka (c)/(d) onu kilitliyor.
   `journalctl | grep "truth: harness düzeltmesi"` ile izlemeye devam.
4. **Higgs çalıştırma yolu resmi değil** — `multimodalart/...-transformers` paketlemesi
   (`trust_remote_code`). Ağırlıklar bayt bayt aynı. Kullanıcı "yamalı olmasın, resmi
   SGLang-Omni'ye geçelim" dedi. **Araştırıldı: SGLang dokümanı yalnız H100 anlatıyor,
   Ampere/sm_86 desteği belirsiz.** Denemeden geçilmemeli.
5. Ortak odada `finalize()`/`consolidate_if_needed` kapalı (bilerek).
6. **`.env` modül-seviyesi okuma tuzağı (`truth_check`).** `agent.py` `load_dotenv()`'i
   import'lardan SONRA çağırıyor → `CLAIM_CHECK_ENABLED/URL/MODEL/TIMEOUT/MAX_CHARS`
   `.env`'den DEĞİL koddaki varsayılanlardan geliyor olabilir. `barge` için
   `reload_settings()` ile çözüldü; `truth_check` için AYNI desen uygulanmalı.
   Doğrulama: `.env`'e `CLAIM_CHECK_ENABLED=false` yazıp yargıcın gerçekten sustuğunu
   ölç. (27 Tem 21:20'de barge tarafında ölçülerek bulundu.)
7. Kaybolan Home Assistant notu hâlâ `2026-07-26-canli-dogrulama.md` sonunda —
   `2026-07-26-canli-dogrulama.md` sonunda — hafıza artık çalıştığı için Candan'a tekrar
   söyletilip kaydettirilebilir (aynı zamanda hafıza düzeltmesinin canlı testi olur).

---

## 5. SIRADAKİ PLAN

1. ~~**Duygu demosunu dinle**~~ ✅ YAPILDI (15:11) — kullanıcı dinledi, tek düzeltme
   `[surprise-*]` → `<|emotion:awe|>`, deploy edildi. Bkz. §3.
2. **Duygu atlası — ayrı proje, yavaş geliştiriliyor.** `experiments/duygu-atlasi/`.
   Ölçüm "anlaşılıyor mu"yu söylüyor, "doğru DUYULUYOR mu"yu söylemiyor; üstelik her
   token bugüne kadar aynı nötr cümlede dinlendi. Atlasta katalogdaki 43 token'ın her
   biri ONA UYGUN bir Türkçe cümlede + 8 tadımlık kombo, etiketli/düz eşiyle.
   **1. tur BİTTİ**: 88 wav, canlı streaming ucundan (`./serve.sh` → 8010).
   Kulak notları ALINDI (44 satır: 28 iyi, 12 idare, 4 kötü), beğenilen 4 kombo
   ÖLÇÜLDÜ (hepsi TEMİZ) ve kombo kulak seti de DİNLENDİ — beşinin beşi de seçildi
   (§3h). ✅ **Eşleme UYGULANDI (22:08, §3i):** üç kombo + beş yeni etiket canlıda,
   `awe` tamamen çıktı. **Sırada: kullanıcının canlı kulak testi** (§3i son madde).
3. **Hız setini dinle** — `experiments/konusma-hizi/serve.sh` (8011). Kademe aralığı
   ölçümle değil KULAKLA seçilir: hangisi "biraz daha hızlı" demek, tavan yeterli mi.
4. **Kalabalık ortam kararı** — wake mi, konuşmacı kapısı mı? Wake seçilirse parantezli
   not hatası ÖNCE düzeltilmeli.
5. **SGLang-Omni araştırması** — 3090'da çalışıyor mu, deneyerek. Çalışıyorsa resmi yola
   geçiş + gerçek streaming.
6. Compaction beklemesini gerçekten paralelleştirmek.
7. **Vurgu setini dinle** — `experiments/vurgu/serve.sh` (8012). Atlasın en çok
   tekrar eden şikâyeti duygu değil VURGU'ydu (5 satır: "olur mu", "on dakikaya",
   "tek başına", "anlamadım", "da"). Ölçüldü (`handoff/2026-07-28-vurgu.md`,
   4 cümle × 7 yol × 8 örnek, canlı uç): **katalogda kelime düzeyinde vurgu YOK**
   — uydurma `<|prosody:emphasis|>` ve SSML `<emphasis>` harfi harfine okunuyor.
   Dolaylı yollardan tek tutarlı olan `<|prosody:pause|>` (hedeften önce,
   bitişik): hedefin perde tepesi 4/4 cümlede +1.2 yarım ton, WER 0.000, baş
   yeme 0/32 — ama kelimeyi UZATMIYOR. Büyük harf çalışmıyor (perde −0.88),
   noktalama uzatıp söndürüyor, kombo (`pause`+`tire`) birbirini siliyor.
   ~~**Karar kulakta**; eşlemeye hiçbir şey eklenmedi.~~ ✅ **KAPANDI:** kullanıcı
   üç tur dinledi, `tire-on` yolunu seçti, 28 Tem 00:53'te eşlemeye ve prompt'a
   girdi (§3j). Kalan iş: **canlıda kulakla dinlemek.**

---

## 6. GERİ DÖNÜŞ

🗑️ **OmniVoice 28 Tem'de sistemden KALDIRILDI** (sunucu + kod). Geri dönüş kolu
ARTIK YOK: `omnivoice-bridge.service`, `/opt/omnivoice*`, HF modeli ve
`worker/omnivoice_tts.py` silindi, `TTS_ENGINE` dallanması kalktı. Gerekçe kullanıcının
kararı (27 Tem): *"OmniVoice çok büyük ve ağır bir model, RAM'de o kadar yerimiz yok.
Gerekirse Piper kurarız."* **Higgs tek ve kalıcı motor.** Streaming sorun çıkarırsa tek
geri dönüş kolu `HIGGS_STREAM=0` (ölçüldü, çalışıyor). TTS'te kalıcı arıza olursa yol
**Piper kurmak** — ~~henüz kurulu DEĞİL~~ ✅ **28 Tem'de KURULDU ve ÖLÇÜLDÜ, ama
CANLIYA BAĞLANMADI** (`handoff/2026-07-28-piper.md`, `experiments/piper-emniyet/`).
`/opt/piper-venv` + `/opt/piper` = **483 MB**, CPU/ONNX, GPU kullanmıyor, ~225 MB RAM.
Sürpriz sonuç: WER **0.0281** — bugünkü canlı Higgs'le **birebir aynı** (RTF 0.031,
16 kat hızlı; ilk ses 0.15 s). Kaybedilen anlaşılırlık DEĞİL **ses kimliği** (klonlama
yok, tek sabit `tr_TR-dfki-medium` sesi) ve **43 duygu/kontrol token'ının tamamı**.
`trnorm` Piper için ŞART (ham metinde WER 0.107 → trnorm 0.028) ve Piper yolunda
`[...]` etiket temizliği eklenmeli (Piper etiketi harfi harfine okur).
**Sırada: kullanıcının kulak testi** — `cd experiments/piper-emniyet && ./serve.sh`
(:8013, 8 cümle Higgs ↔ Piper yan yana, JSON kopyalamalı). Bağlama önerisi: otomatik
yedek DEĞİL, **elle çevrilen `.env` kolu** (gerekçe raporda §4.2). Vazgeçilirse tek
blokla silinir: `ssh root@192.168.0.25 'rm -rf /opt/piper /opt/piper-venv /opt/piper-exp'`.
⚠️ Candan'ın ses kimliği (`default-ref.wav`) artık `/opt/candan-lite/assets/voice/`
altında; ayrıntı ve geri getirme: `handoff/2026-07-28-omnivoice-kaldir.md`.

```bash
# Streaming'i kapat (kod kalır) — TTS'te ilk başvurulacak kol

ssh root@192.168.0.25 'echo "HIGGS_STREAM=0" >> /opt/candan-lite/worker/.env && systemctl restart candan-worker'

# Konuşma hızı kolunu KAPAT (kod kalır, ses bugünküyle bire bir aynı olur)
ssh root@192.168.0.25 'echo "SPEECH_SPEED=0" >> /opt/candan-lite/worker/.env && systemctl restart candan-worker'

# Şaşırma tonunu geri al (awe → surprise, 15:11 değişikliği)
ssh root@192.168.0.25 'cd /opt/candan-lite && \
  cp worker/higgs_tts.py.bak-sasirma-20260727 worker/higgs_tts.py && \
  rm -f worker/data/tts-cache/*.pcm && systemctl restart candan-worker'

# Wake gate'i bugünkü gibi AÇIK tut (27 Tem env düzeltmesi `.env`'i gerçekten okunur
# yaptı; `.env`'de WAKE_ENABLED=false yazıyordu ve artık ETKİLİ)
ssh root@192.168.0.25 'sed -i "s/^WAKE_ENABLED=false/WAKE_ENABLED=true/" \
  /opt/candan-lite/worker/.env && systemctl restart candan-worker'

# `.env` kolları düzeltmesini tümden geri al
ssh root@192.168.0.25 'cd /opt/candan-lite/worker && \
  for f in agent pi_brain truth_check reminders; do cp -a $f.py.bak-envkol-20260727 $f.py; done && \
  systemctl restart candan-worker'
```
Sunucudaki yedekler: `*.bak-20260726`, `*.bak-20260727`, `*.bak-d2b1b4d`,
`*.bak-hiz-20260727` (konuşma hızı — tam blok o notta).
Referans ses: `/opt/candan-lite/assets/voice/default-ref.wav` (+ bu Mac'te
`assets/voice/default-ref.wav`) — md5 `429e867bcad6cf6e8c4109efe88f5e6f`.
Eski `/opt/omnivoice/…bak-20260726` kopyası birebir aynıydı, OmniVoice ile silindi.
Git'te her düzeltme ayrı commit — tek tek geri alınabilir.

---

## 7. ÇALIŞMA NOTLARI (yeni oturum için)

- **Sunucuya deploy serbest** (kullanıcı 26 Tem'de yetkilendirdi) ama: yedek → doğrula →
  restart → log temiz mi → md5 karşılaştır. Geri dönüşü TEK BLOK yaz.
- **Kullanıcının gerçek verisine test amaçlı yazma.** 27 Tem'de bir test notu gerçek
  hafızaya yazıldı (uzantı hafıza kökünü `MEMORY_DIR`'den değil ÇALIŞMA DİZİNİNDEN
  çözüyor). İzolasyon için `MEM_DIR` kullan, sonra sızıntı olmadığını doğrula.
- **Mikrofon gerekmez:** beyin/hafıza/mod testleri metin enjeksiyonuyla yapılabilir
  (`PiRpcClient` + `write_turn_user`). Yöntem: gece raporu §2a.
- ⚠️ **`worker/` içinde MODÜL SEVİYESİNDE env okuma = `.env` ETKİSİZ.** `agent.py`'nin
  `load_dotenv()`'i import'lardan SONRA çalışır (systemd `EnvironmentFile=` kullanamıyor:
  boşluklu Türkçe değerler ayrıştırıcıyı kırıyor). Bir ayar **kol olacaksa**
  `reload_settings()` deseni ŞART ve çağrısı `agent.py`'de `load_dotenv`'in altına
  eklenmeli. Ayrıca: varsayılan argümanlar tanım anında bağlanır
  (`def f(x=SETTING)` → `def f(x=None)`), ve `from X import NAME` **değer kopyalar** →
  tazeleme kopyaya yansımaz. Bu üç tuzağı `tests/test_env_kollari.py` kilitliyor
  (modül seviyesinde env okuyup `reload_settings()`'i olmayan modül varsa test DÜŞER).
  Denetim tablosu + `.env`'den ayarlanamayanlar: `2026-07-27-env-kollari.md`.
- **Ölç, tahmin etme.** Bu iki günde altı yanlış teşhis ölçümle düzeltildi: bellek
  sızıntısı (yoktu), endpointing (adaptifmiş), tanıma eşiği (hataymış), dev modun tek
  suçlu olması (normal mod da bozuktu), sınıflandırıcı maliyeti (abartılmış), RTF
  bozulması (kendi test yüküm).
