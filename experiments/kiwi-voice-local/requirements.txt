# Kiwi Voice has no usable runtime dependency metadata, so the speaker-ID-only
# experiment declares the minimal imports explicitly. setup.sh checks out Kiwi.
numpy>=1.24,<3
sounddevice>=0.4.6
PyYAML>=6
# pyannote.audio 3.x, TorchAudio 2.6+'da kaldırılan AudioMetaData API'sini kullanıyor.
torch==2.5.1
torchaudio==2.5.1
pyannote.audio>=3.1,<4
huggingface-hub==0.36.2
librosa>=0.10
